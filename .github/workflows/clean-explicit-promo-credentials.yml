name: Clean Explicit Promo Credentials
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/clean-explicit-promo-credentials.yml'
      - 'scripts/clean_explicit_promo_credentials.py'
  workflow_dispatch:
concurrency:
  group: markdown-temp-repair-main
  cancel-in-progress: false
permissions:
  contents: write
jobs:
  clean:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Rebase to latest main
        run: |
          git fetch origin main
          git rebase origin/main
      - name: Clean only explicit credentials and known watermarks
        run: python scripts/clean_explicit_promo_credentials.py
      - name: Refresh audits
        run: |
          python scripts/scan_explicit_promo_credentials.py
          python scripts/summarize_explicit_promo_tokens.py
          python scripts/audit_temp_semantic_completeness.py --root temp --report temp/.semantic-completeness-audit.json
          python scripts/summarize_semantic_audit_issues.py
      - name: Commit cleaned Markdown and reports
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add temp scripts/clean_explicit_promo_credentials.py
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'docs(temp): remove explicit promo credentials and watermarks [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
