name: Extract Human Business Semantic Anchors

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/extract-human-business-semantic-anchors.yml'
      - 'scripts/extract_human_business_semantic_anchors.py'
  workflow_dispatch:

concurrency:
  group: markdown-temp-repair-main
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  extract:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Rebase to latest main
        run: |
          git fetch origin main
          git rebase origin/main
      - name: Extract source-backed anchors
        run: |
          python scripts/extract_human_business_semantic_anchors.py
          cat temp/.human-business-semantic-anchors.json
      - name: Commit diagnostic
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add temp/.human-business-semantic-anchors.json
          if git diff --cached --quiet; then
            echo 'Semantic anchors unchanged.'
          else
            git commit -m 'chore(temp): refresh human-business semantic anchors [skip ci]'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
